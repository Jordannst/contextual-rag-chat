feat(backend): upgrade Gemini model dan tambah tooling untuk model management

**Perubahan Utama:**

1. **Upgrade Model Gemini untuk Chat:**
   - Update dari `gemini-1.5-flash` ke `gemini-2.0-flash` sebagai primary model
   - Implement fallback chain: gemini-2.0-flash-001 → gemini-flash-latest → gemini-2.5-flash
   - Fix error 404 saat model tidak tersedia dengan automatic fallback

2. **Environment File Handling:**
   - Tambah `utils/env.go` untuk handle BOM (Byte Order Mark) pada .env file
   - Support multi-location .env file search (current dir, parent dirs)
   - Update semua entry points (main.go, cmd/create-db, cmd/migrate) untuk gunakan LoadEnvWithBOMHandling()

3. **Model Management Tool:**
   - Tambah `cmd/check-models/main.go` untuk melihat daftar model Gemini yang tersedia
   - Tool ini membantu verify model name yang valid untuk API key
   - Tampilkan rekomendasi model untuk chat RAG

4. **Documentation:**
   - Tambah `MODELS.md` dengan dokumentasi lengkap model Gemini
   - Penjelasan embedding vs generative models
   - Rekomendasi penggunaan untuk berbagai use case

5. **Logging & Debugging Improvements:**
   - Tambah detailed logging di `handlers/chat.go` untuk setiap step RAG flow
   - Tambah logging di `handlers/upload.go` untuk tracking chunk processing
   - Improved error messages dengan step-by-step tracking

6. **Code Quality:**
   - Perbaikan error handling di `db/db.go`
   - Update PDF extraction di `utils/document_extractor.go`
   - Konsistensi error handling di seluruh codebase

**Files Changed:**
- Modified: utils/chat.go, handlers/chat.go, handlers/upload.go, main.go
- Modified: cmd/create-db/main.go, cmd/migrate/main.go, db/db.go
- Modified: utils/document_extractor.go, scripts.ps1, go.mod, go.sum
- Added: utils/env.go, cmd/check-models/main.go, MODELS.md

**Breaking Changes:** None
**Migration Required:** None

